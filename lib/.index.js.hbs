/**
 * {{pkg.description}}
 * @module {{pkg.name}}
 * @version {{pkg.version}}
 */

'use strict'

const define = require('./define')
const fork = require('./fork')

let lib = define.bind(this)

Object.assign(lib, define, {
  define,
  fork
})

module.exports = lib
